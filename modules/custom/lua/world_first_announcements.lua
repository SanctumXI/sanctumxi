-- Replaced by modules/sanctum/ServerFirst.lua.
--
-- Unfortunately, the old method has a 'triggered on last hit' effect which means it would double up with the new system, so it has to get deleted and replaced.
return {}
